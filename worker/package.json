{
  "name": "agenticly-blog-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that serves the GitHub Pages Jekyll blog at agenticly.app/blog",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
